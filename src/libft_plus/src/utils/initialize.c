#include "alloc.h"
#include <stdio.h>
#include <string.h>

void initialize_heap(t_heap *heap) {
  /*memset(heap, 0, sizeof(t_heap));*/

  (void)heap;
  /*heap->free_size = 0;*/
  /*printf("Size: %lu\n", heap->block_count);*/
  /*printf("Size: %lu\n", heap->total_size);*/
  /*printf("Size: %lu\n", heap->free_size);*/
}
